# quick start
```python install_dot_files.py```
- create symlink for each file/directory(all file/directory starts with ".") within each directory:
  - ~/.bashrc -> bash/.bashrc
  - ~/.gitconfig -> git/.gitconfig
  - ...
